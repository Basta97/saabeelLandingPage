import { Component } from '@angular/core';
import { SectionTitle } from '../../common/section-title/section-title';

@Component({
  selector: 'app-futuref',
  imports: [SectionTitle],
  templateUrl: './futuref.html',
  styleUrl: './futuref.css',
})
export class Futuref {

}
